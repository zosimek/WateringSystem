<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Green color -->
    <stroke
        android:color="@color/theme_green"
        android:width="5dp"/> <!-- Green color -->
    <corners android:radius="20dp" />
</shape>