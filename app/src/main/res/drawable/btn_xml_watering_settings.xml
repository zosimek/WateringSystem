<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/theme_green" />
    <corners android:radius="0dp" />
    <stroke
        android:width="2dp"
        android:color="@color/theme_green" />
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
    <item android:drawable="@drawable/btn_watering_settings" /> <!-- Replace with the actual name of your PNG image file -->
</shape>